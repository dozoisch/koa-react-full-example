// import { Document } from "mongoose/lib/browser";
// import CountSchema from "../../shared/schema/CountSchema";
export default (obj) => {
  // @TODO weird mongoose issue
  return obj;
  // return new Document(obj, CountSchema);
}
